# Corpus split statistics and leakage audit

Generated by `just data` (packages/training/.../data/pipeline.py) - do not hand-edit.

Generated: 2026-09-03T15:42:36Z

## Partitions

| partition | rows | sources |
|---|---:|---|
| train | 16030 | promptshield=16030 |
| validation | 790 | promptshield=790 |
| test | 19937 | promptshield=19937 |
| cross_source_eval | 1694 | deepset_prompt_injections=580, jackhhao_jailbreak=1114 |
| hard_negatives_eval | 70 | hard_negatives=70 |

## Leakage remediation (this pipeline's own dedup pass)

- Cross-partition near-duplicate clusters found and remediated: **101**
- Within-partition exact/near duplicates collapsed: **1568**
- Rows dropped in remediation: **6942**

## Independent leakage audit (re-derived, not trusted from the splitter)

Final partitions are **clean** - no near-duplicate cluster has members in more than one partition.

## Naive random-split comparison

What a naive row-level 80/10/10 split (ignoring source and near-duplicate structure entirely) would have produced, computed over the same pool:

- Fraction of the naive test partition that would share a near-duplicate cluster with something in the naive train partition: **17.4%**
- This pipeline's family-disjoint split: **0%** by construction (see audit above).

This is the concrete cost of the row-level split this project's non-negotiables explicitly reject (ADR-0004): the naive number above is not hypothetical, it is measured on this exact corpus.
